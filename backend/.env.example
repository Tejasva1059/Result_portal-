# ==============================================================================
# NEW SUNSHINE PUBLIC SCHOOL - RESULT MANAGEMENT PORTAL
# HYBRID / OFFLINE-FIRST CONFIGURATION TEMPLATE
# ==============================================================================

# Project Details
PROJECT_NAME="New Sunshine Public School Portal"
SECRET_KEY="new_sunshine_public_school_super_secret_jwt_key_2026_indore_73181"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ------------------------------------------------------------------------------
# 1. LOCAL OFFLINE MODE (DEFAULT - ZERO SETUP REQUIRED)
# ------------------------------------------------------------------------------
# Uses the built-in SQLite engine stored in the backend directory.
# Completely offline, 0ms latency, zero database installation required.
DATABASE_URL="sqlite:///./school_portal.db"

# ------------------------------------------------------------------------------
# 2. LOCAL LAN POSTGRESQL MODE (OPTIONAL SCHOOL SERVER)
# ------------------------------------------------------------------------------
# If you run a local PostgreSQL server on the school computer/LAN:
# DATABASE_URL="postgresql://postgres:password@localhost:5432/school_portal_db"

# ------------------------------------------------------------------------------
# 3. ONLINE / CLOUD DEPLOYMENT MODE (AWS / Render / Supabase / Neon / DigitalOcean)
# ------------------------------------------------------------------------------
# When deploying to a cloud host with SSL enabled:
# DATABASE_URL="postgresql://user:secure_password@cloud-db-host:5432/school_portal_db?sslmode=require"
